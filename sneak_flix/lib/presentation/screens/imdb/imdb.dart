import 'package:flutter/material.dart';

class Imdb extends StatelessWidget {
  const Imdb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Contenido IMDB'),
      ),
    );
  }
}